#### maven 各种命令使用
```bash
mvn clean #
mvn install #下载安装jar包
mvn compile #编译成字节码文件，相当于build
mvn package #生成jar
mvn deploy #上传生成的jar包

mvn dependency:list：列出项目的依赖‌
mvn dependency:tree：显示项目的依赖树‌
mvn dependency:analyze：分析项目的依赖情况‌
```
